# Core (needed now, for voice + orchestration)
requests>=2.31.0

# YouTube upload (install only when we wire uploads)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
